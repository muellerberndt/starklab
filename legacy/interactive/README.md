# Interactive prototype (Toy VM → Trace → AIR)

Open `docs/interactive/index.html` in a browser.

If you run into issues with local file loading, serve it:

```bash
cd docs/interactive
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

